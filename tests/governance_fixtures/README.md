# Governance regression fixtures

These fixtures are intentionally small, deterministic, and standard-library friendly.
They support `tests/test_governance.py` without network access, pytest, or heuristic behavior.
